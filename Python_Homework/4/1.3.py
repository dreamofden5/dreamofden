tags = {
    "python" if tag.lower() == "python" else tag.lower()
    for tag in [
        "Django",
        "FastAPI",
        "Numpy",
        "PYTHON",
        "Pandas",
        "FASTAPI",
        "Python",
        "random",
    ]
}
print(tags)
